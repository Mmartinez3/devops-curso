name: demo github action

on:
  push:
    branches: ["devops-simple-node1"]
  pull_request:
    branches: ["devops-simple-node1"]

  workflow_dispatch:
  
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: listar
          run: ls -a
